# ============================================================================
# JUKEBOX BACKEND - ENVIRONMENT CONFIGURATION TEMPLATE
# ============================================================================
#
# This file is a TEMPLATE. Do NOT commit it with real credentials.
#
# For Local Development:
#   1. Copy: cp .env.example .env.local
#   2. Edit: nano .env.local
#   3. Add your credentials (GEMINI_API_KEY, REPLICATE_API_TOKEN, etc.)
#
# For Production (Cloud Run):
#   1. Copy: cp .env.example .env.prod
#   2. Edit: nano .env.prod (non-sensitive vars only)
#   3. Secrets (GEMINI_API_KEY, etc.) come from Google Secret Manager
#
# The system automatically loads the correct file based on ENV:
#   - ENV=local or development → loads .env.local
#   - ENV=production           → loads .env.prod
#
# ============================================================================

# Environment mode: local, development, or production
ENV=local

# Google Cloud Platform
GCLOUD_PROJECT_ID=your-gcp-project-id
GCLOUD_PROJECT_NUMBER=your-gcp-project-number
GCLOUD_MUSIC_BUCKET=prompts_results

# AI/ML Models and APIs
# ⚠️  IMPORTANT: These are SECRETS - NEVER commit with real values!
GEMINI_API_KEY=your-gemini-api-key-here
REPLICATE_API_TOKEN=your-replicate-api-token-here
IMAGE_GENERATION_LOCATION=us-central1
GEMINI_MODEL=gemini-1.5-flash
IMAGEN_MODEL=imagen-3.0-generate-001

# Firestore Collections
INSTRUMENTS_COLLECTION=instrument
JUKEBOX_COLLECTION=jukebox
MUSIC_SUB_COLLECTION=musics

# Optional: External Services
MUSICGEN_URL=http://localhost:8000

# Optional: Email Service (for notifications)
# If not used, set to dummy value to avoid crashes
GOOGLE_APP_EMAIL=
GOOGLE_APP_PASSWORD=dummy

# Optional: GitHub Integration
GITHUB_ACCESS_TOKEN=

# Database (adjust if using different credentials)
SQLALCHEMY_DATABASE_URI=postgresql+asyncpg://postgres:postgres@localhost:5434/jukebox_db

